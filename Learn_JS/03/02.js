if(true){
    // 참이면 형변환
    console.log("참!");
}

const age = 20;
// const message = age >= 18 ? "성인입니다." : "";
// const message = age >= 18 && "성인입니다.";
if(age >= 18){
    console.log("성인입니다.");
}else{
    console.log("미성년자입니다.");
}

// 조건의 처리 결과가 필요하면 삼항연산자
// 조건에 따른 분기 처리가 필요하면 조건문

